# Required for the contact form to send mail via Resend.
# Create a free account at https://resend.com and generate an API key.
RESEND_API_KEY=

# Optional — sender address for outgoing contact-form emails.
# Defaults to Resend's shared sandbox sender if unset.
CONTACT_FROM_EMAIL=
